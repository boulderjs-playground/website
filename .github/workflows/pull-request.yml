name: Pull Request

on:
  push:
    branches:
      - '**'
      - '!main'

jobs:
  test:
    uses: ./.github/workflows/test.yml
    secrets: inherit

  coverage:
    uses: ./.github/workflows/coverage.yml
    secrets: inherit
